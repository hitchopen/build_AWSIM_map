# ---------- macOS ----------
.DS_Store
.AppleDouble
.LSOverride
._*
Icon?
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---------- Editors / IDEs ----------
.vscode/
.idea/
*.swp
*.swo
*~

# ---------- AWSIM map debug outputs ----------
# `--save-ego-debug-pcd` writes this companion file showing the points the
# vehicle-self filter rejected. Regenerable, so don't commit it.
map/**/ego_returns.pcd

# ---------- sample-track sensor blobs (~90 GB each) ----------
# Fetched on demand by each track's data/<track>/download.sh — too large
# for git/LFS. Covers raw.db3, partial downloads (raw.tar.xz / raw.zip), and
# any of the well-known archive wrappers OneDrive may use during transfer.
data/**/raw.db3
data/**/raw.tar.xz
data/**/raw.tar.gz
data/**/raw.zip

# ---------- generated MCAP bags (anywhere in the repo) ----------
# Every .mcap is a derived artefact — `cleaned_tf.mcap` is regenerable from
# `raw.db3` via convert_db3_to_mcap.py, and any test slices written to
# /tmp/... or under data/ are likewise reproducible. Never commit them.
*.mcap
